[Unit]
Description=Pre-start Distrobox utils container
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=distrobox enter utils -- sleep 1
RemainAfterExit=yes

[Install]
WantedBy=graphical-session.target
